# 技术特征对照表

**项目**：烟花爆竹全链条数字化预警监管平台 —— 风险智能识别系统
**文件用途**：发明专利技术交底书的**代码附件对照说明**
**源码包**：`patent-src/`（模块化工程源码，14 个算法单元 + 渲染层）
**对照基准**：`patent-model.js`（旧版单文件数据模型）与线上运行的 `patent-s1~s5.html`
**版本 / 日期**：v1.0.0 / 2026-09-18

---

## 〇、阅读方法

1. 本表把方案拆成 **7 组技术特征（S0~S6，共 27 项）**。每项特征给出：技术内容 → 实现文件与行号 → 关键函数 → 关键参数 → 技术效果。
2. 行号格式为 `文件路径:行号`，相对 `patent-src/` 目录。
3. 特征编号 `S1`~`S5` 与页面编号、功能阶段编号一致，可直接对应到 `pages/s1.html`~`pages/s5.html` 五个功能页。
4. 全部阈值参数集中在 `js/core/01-params.js`，是该方案唯一的数值边界来源，方便审查与调整。

---

## 一、系统级技术方案与代码结构对应

| 层次 | 职责 | 对应目录 | 是否含创新点 |
| --- | --- | --- | --- |
| 数据模型层 | 5 项核心计算与 2 条风险判定式、时间线预计算、状态聚合 | `js/core/00~13-*.js` | **是**（S0~S6 全部特征） |
| 接口兼容层 | 把 `window.FWCore` 导出为 `window.PATENT` | `js/compat/export-patent.js` | 否 |
| 渲染框架层 | 监测时钟、主循环、等比缩放、单次状态分发 | `js/render/page-shell.js`、`boot.js` | 部分是（S6-2、S6-4） |
| 页面实现层 | 5 个功能页的界面渲染 | `js/pages/s1~s5.js` | 否 |
| 样式 | 深蓝科技感指挥大屏样式 | `css/platform.css` | 否 |
| 外部依赖 | 江西省行政区划 GeoJSON（公开数据）、ECharts 5.4.3 | `../../geo-data.js`、CDN | 否 |

**加载顺序**（`tools/build-pages.py` 中 `CORE_MODULES` 为唯一真源）：

```
00 → 01 → 02 → 03 → 04 → 05 → 06 → 07 → 08 → 09 → 10 → 11 → 12 → 13
→ compat/export-patent.js → render/page-shell.js → pages/sN.js → render/boot.js
```

---

## 二、技术特征逐项对照

### 表 1　S0 参数体系与阶段划分

| 特征编号 | 技术特征 | 实现位置 | 关键函数 / 语句 | 关键参数 | 技术效果 |
| --- | --- | --- | --- | --- | --- |
| **S0-1** | 判定阈值**集中参数化**，与算法实现完全分离 | `js/core/01-params.js:22` | `var P = { … }` | `capacity 800 / fullRatio 0.85 / staticSpeed 2 / tolerance 15 / stayThreshold 0.60 / confThreshold 0.85 / fenceR 0.0082 / bookDrop 0.15 / stock0 498` | 阈值整组替换即可适配不同仓储类型（1.3 级工库房、中转库、零售库），无需改动任何算法代码 |
| **S0-2** | 五阶段**功能时间线**：数据获取 → 特征构造 → 风险判定 → 校验预警 | `js/core/01-params.js:47`、`js/core/12-aggregate.js:58` | `TL`、`stageAt(t)` | `s1[0,9] / s2[9,17] / s3[17,32] / s4[32,41] / s5[41,56] / end 60` | 同一条数据流被复用为 5 个相互印证的判断依据，且各阶段界面可独立展示而数值完全一致 |

### 表 2　S1 库存饱和度计算

| 特征编号 | 技术特征 | 实现位置 | 关键函数 / 语句 | 关键参数 | 技术效果 |
| --- | --- | --- | --- | --- | --- |
| **S1-1** | 由**扫码出入库流水**按时刻单向累加/扣减求库存量 Q(t)，出库以下限 0 截断 | `js/core/06-s1-saturation.js:27` | `stockAt(t)` | `P.stock0 = 498`（T+0 初始库存） | 把离散的扫码事件还原为连续库存曲线，是后续全部判定的数据基础 |
| **S1-2** | **归一化饱和度** S(t) = Q(t) / C，与仓储规模解耦 | `js/core/06-s1-saturation.js:42` | `satAt(t)` | `C = 800 吨` | 不同库房可用同一组阈值判定，判定阈值具备通用性 |
| **S1-3** | 扫码流水**可回溯快照**：截止 t 的最近 N 笔记录与入库/出库累计笔数 | `js/core/06-s1-saturation.js:50`、`:64` | `scansAt(t, cap)`、`scanCount(t)` | `cap` 默认 26 笔 | 饱和度数值可逐笔追溯到原始扫码记录，满足监管场景的可审计要求 |

### 表 3　S2 电子围栏与车辆轨迹

| 特征编号 | 技术特征 | 实现位置 | 关键函数 / 语句 | 关键参数 | 技术效果 |
| --- | --- | --- | --- | --- | --- |
| **S2-1** | 由**行政区划边界数据逐级解析**定位目标仓储，围栏/轨迹/标注共用同一坐标系 | `js/core/03-geo-anchor.js:81`、`:90` | `centroidOf`、`bboxOf`、`CENTER`、`BBOX`、`WARE` | 省 → 市（`360000`→宜春市）→ 县（万载县）→ 质心 + (0.004, 0.006) 北偏 | 同一套算法可迁移到其他县域，仅改目标县名称，无需重新标定坐标 |
| **S2-2** | 车辆实体建模：车牌 / 车型 / 停靠点 / 进出场路径 / 电子运单 | `js/core/05-vehicles.js:32` | `VEHICLES`、`pathFor`、`park`/`exit` 推导 | 方位角 `2πi/n + 0.55`，半径 `0.0034 + (i mod 3)×0.0017` | 车辆分布可复现、不依赖随机数；围栏内多车标记互不遮挡 |
| **S2-3** | 车辆轨迹表示为**时刻分段函数**：未进入 → 驶入（34→16 km/h）→ 停靠（→0）→ 驶离（8→34 km/h） | `js/core/07-s2-geofence.js:38`、`:62` | `vehPos(v, t)`、`vehExitStart(v)` | `TRAVEL = 3.0 min`、`UNLOAD = 6.0 min` | 轨迹连续可积，停留时间与瞬时速度可由同一函数导出 |
| **S2-4** | **围栏内车辆集合**：按平面距离 ≤ 围栏半径 × 1.6 判定 | `js/core/07-s2-geofence.js:82` | `inFenceAt(t)` | `P.fenceR = 0.0082`（≈800 m） | 把「在库区内」这一定性判断变为可计算的几何判据 |
| **S2-5** | **驶离轨迹监测**：只统计考察窗口起点之后才开始驶离的车辆 | `js/core/07-s2-geofence.js:70` | `detectDeparture(t)` | `BOOK_WIN = 36.8 min` | 区分「正常装卸后离场」与「风险窗口内离场」，显著降低误报 |

### 表 4　S3 滞留车辆筛选与异常滞留指数

| 特征编号 | 技术特征 | 实现位置 | 关键函数 / 语句 | 关键参数 | 技术效果 |
| --- | --- | --- | --- | --- | --- |
| **S3-1** | 车辆行驶时序：进入围栏 → 行驶 TRAVEL → 停靠 UNLOAD（正常车）或长期停靠（滞留车）→ 驶离 | `js/core/05-vehicles.js:26`、`:29` | `TRAVEL`、`UNLOAD`、`stay` 标记 | 3.0 min / 6.0 min | 为「停靠时长」提供了物理可解释的基准，使容忍阈值可标定 |
| **S3-2** | **滞留车辆集合 V_stay(t)**：停留时长 ≥ 容忍阈值 **且** 速度 < 静止阈值（双条件「与」） | `js/core/08-s3-stagnation.js:55` | `isStay`（`stagnationAt` 内） | `tolerance = 15 min`、`staticSpeed = 2 km/h` | 把「正在正常装卸的车辆」与「长时间停靠且已停止移动的车辆」区分开 |
| **S3-3** | **异常滞留指数 I(t)**：双归一化权重（时长权重 + 静止程度权重）等权平均后求和归一化 | `js/core/08-s3-stagnation.js:50-52`、`:66` | `wStay`、`wSlow`、`weight`、`index` | `wStay = min(1, stayMin/15)`；`wSlow = max(0, 1 − spd/2)`；`I = min(1, Σw/3 × 0.78 + (Σw>0 ? 0.1 : 0))` | 把「是否滞留」的定性判断升级为连续可比的异常程度指标，可与饱和度做同量级阈值比较 |
| **S3-4** | 滞留**明细行**：车牌 / 进入时刻 / 围栏内时长 / 速度 / 权重 / 运单 / 货物 / 驾驶员 / 轨迹阶段 / 坐标 | `js/core/08-s3-stagnation.js:58-64` | `rows.push({…})` | 12 个字段 | 单一指数可下钻到具体车辆与运单，形成从指标到证据的完整链条 |

### 表 5　S4 双风险判定（核心特征）

| 特征编号 | 技术特征 | 实现位置 | 关键函数 / 语句 | 关键参数 | 技术效果 |
| --- | --- | --- | --- | --- | --- |
| **S4-1** | **判定式 A（隐蔽囤货 Risk_Hidden）**：`S(t) ≥ 满载阈值 ∧ I(t) ≥ 滞留报警阈值` | `js/core/09-s4-dual-risk.js:39`；调用点 `js/core/11-timeline.js:60` | `judgeHide(sat, idx)` | `≥ 0.85 ∧ ≥ 0.60` | 库存接近满容 + 车辆停滞不动 ⇒ 以运输车辆为临时储位的「以车代库」囤积。双条件「与」逻辑比单指标判定显著降低误报率 |
| **S4-2** | **账面下降幅度 ΔS**：以判定阶段**峰值**为基准，非负截断 | `js/core/09-s4-dual-risk.js:49`；调用点 `js/core/11-timeline.js:63` | `bookDropOf(peakSat, sat)` | `ΔS = max(0, S_peak − S(t))` | 避免「库存本就低于初始值」造成的天然负差被误判为下降 |
| **S4-3** | **判定式 B（账面造假 Risk_Phantom）**：`ΔS ≥ 下降阈值 ∧ ¬Departure(考察窗口)` | `js/core/09-s4-dual-risk.js:59`；调用点 `js/core/11-timeline.js:70` | `judgeBook(bookDrop, hasDeparture)` | `≥ 15% ∧ 无驶离轨迹`，`BOOK_WIN = 36.8 min` | 账面批量出库但围栏内无任何车辆驶离 ⇒ 出库登记与实物流不一致，识别虚假出库登记 / 账外转移 |
| **S4-4** | 流水的 **phantom 标记**：登记出库但无对应车辆驶离轨迹 | `js/core/04-op-dataset.js:17`（记录字段）；对照展示于 `js/pages/s1.js` | `o.phantom` | 13 条标记记录 | 使「账面动作」与「物理动作」在同一条流水上直接对照，形成可出示的证据 |
| **S4-5** | 判定结果**置位保持（latch）**语义：首次命中即记录时刻，后续不再取消 | `js/core/11-timeline.js:48`、`:60`、`:70` | `riskHideFrom`、`bookFrom`、`alertFrom` | `riskHideFrom ≈ 32.0′`、`bookFrom ≈ 40.6′`、`alertFrom ≈ 41.0′` | 风险结论可回溯、不闪烁，避免因瞬时波动导致告警反复 |

### 表 6　S5 置信度校验与管控预警

| 特征编号 | 技术特征 | 实现位置 | 关键函数 / 语句 | 关键参数 | 技术效果 |
| --- | --- | --- | --- | --- | --- |
| **S5-1** | **四路数据源一致性校验**：扫码数据完整性 / 视频监控核验 / 定位轨迹连续性 / 电子运单比对 | `js/core/10-s5-confidence.js:45` | `confidenceAt(t)` → `parts.a/b/c/d` | 区间 `a∈[88,97]`、`b∈[86,98]`、`c∈[90,99]`、`d∈[82,96]` | 把「数据源本身不可靠」与「风险客观存在」区分开，避免单路数据缺失或失真直接触发执法动作 |
| **S5-2** | **加权总置信度** C(t) 与阈值校验门 | `js/core/10-s5-confidence.js:53`；调用点 `js/core/11-timeline.js:78` | `total = 0.32a + 0.24b + 0.26c + 0.18d`；`pass = total ≥ 85` | 权重 0.32/0.24/0.26/0.18，阈值 0.85 | 只有多源校验通过后才确认风险成立并下发预警，形成「判定 → 校验 → 预警」的两级确认机制 |
| **S5-3** | 分路评分按**监测时刻整分钟序号**取值，同分钟内读数稳定、可复现 | `js/core/10-s5-confidence.js:33`、`:46` | `hash01(s)`、`k = Math.round(t)` | 确定性散列，无随机数 | 置信度可复现、可审计；不引入随机数，便于事后复算 |
| **S5-4** | 预警确认后在政务云 **GIS 上分级叠加呈现** 5 个图层 | `js/core/13-map-layers.js:44~189` | `registerMaps`、`fenceSeries`、`trackSeries`、`vehSeries`、`wareSeries`、`alertSeries` | `zlevel 2/3/5/6/7`；围栏虚线圆半径 `fenceR`；仓储超满载阈值转告警色 | 风险结论落到「仓储坐标 + 车辆坐标」的可视位置，直接支撑现场核查；固定 zlevel 保证叠加顺序不随数据更新顺序变化 |

### 表 7　S6 时间线预计算与状态聚合（方法性特征）

| 特征编号 | 技术特征 | 实现位置 | 关键函数 / 语句 | 关键参数 | 技术效果 |
| --- | --- | --- | --- | --- | --- |
| **S6-1** | **全时间线等步长预计算**：加载时一次性求出 [0, 60] 区间全部指标序列 | `js/core/11-timeline.js:40`、`:46` | `GRID_STEP`、`DERIVED`（立即执行函数） | `GRID_STEP = 0.05 min`，共 1201 个采样点 | 每个采样点保存 14 项指标，形成完整的「时刻 → 证据」序列 |
| **S6-2** | **判定与帧序解耦**：判定不依赖渲染帧、播放进度或定时器 | `js/core/11-timeline.js:46` | `DERIVED` 循环（不引用任何界面状态） | — | ①5 个功能页单独打开时判定时刻完全一致；②任意速度播放/暂停/重置/刷新，判定结果不变；③结论可离线复算，满足可审计要求 |
| **S6-3** | 采样点读取：任意连续时刻 **O(1)** 映射到预计算序列 | `js/core/11-timeline.js:101` | `rowAt(t)` | `i = round(t / 0.05)` | 任意时刻查询为常数时间，60 分钟 × 6 页的实时刷新无性能压力 |
| **S6-4** | **单时刻完整状态快照**：一次调用返回全部证据 | `js/core/12-aggregate.js:35`；分发点 `js/render/page-shell.js:51` | `stateAt(t)` | 18 个字段 | 「一个时刻 = 一份完整证据链」；页面每帧只取一次状态，从机制上排除各页取值不一致 |
| **S6-5** | 阶段标注表与代表时刻 | `js/core/12-aggregate.js:68`、`:91` | `stageMeta`、`stageTime(no)` | 代表时刻 `31.5 / 34.0 / 35.0 / 41.5 / 45.0` | 支持总览页与阶段间跳转，可直接跳转到最能体现某一特征的时刻 |
| **S6-6** | 时刻冻结**自检钩子**：可把界面冻结在任意监测时刻并重绘 | `js/render/page-shell.js:111` | `__setT(t)`、`__setPaused(v)`、`__getT()` | — | 使界面比对可复现，为等价性验证提供工程手段 |

---

## 三、实算结果（阈值边界验证）

以下数值由源码包在浏览器中实算得出（`tools/equivalence-test.html` 加载后读取），可用于核对判定式边界：

| 监测时刻 t | 库存量 Q(t) | 饱和度 S(t) | 滞留车辆数 | 异常滞留指数 I(t) | 账面下降 ΔS | 置信度 C(t) | 隐蔽囤货 | 账面造假 | 管控预警 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| T+0′ | 498.0 吨 | 62.25% | 0 | 0 | — | 95.84 | 否 | 否 | 否 |
| T+20.0′ | 602.5 吨 | 75.31% | 0 | 0 | — | 89.94 | 否 | 否 | 否 |
| T+31.9′ | 682.7 吨 | 85.34% | 3 | 0.88 | — | 90.38 | 否（未进入判定阶段） | 否 | 否 |
| **T+32.0′** | 682.7 吨 | **85.34% ≥ 85%** | 3 | **0.88 ≥ 0.60** | 0 | 90.38 | **✅ 是** | 否 | 否 |
| T+35.0′ | 688.5 吨 | 86.06% | 3 | 0.88 | 0.58% | 90.38 | 是 | 否 | 否 |
| T+40.5′ | 586.7 吨 | 73.34% | 3 | 0.88 | 13.75% | 90.94 | 是 | 否（未达 15%） | 否 |
| **T+40.6′** | 564.3 吨 | **70.54%** | 3 | 0.88 | **16.55% ≥ 15%** | 90.94 | 是 | **✅ 是** | 否 |
| **T+41.0′** | 564.3 吨 | 70.54% | 3 | 0.88 | 16.55% | **90.94 ≥ 85** | 是 | 是 | **✅ 下发** |
| T+45.0′ | 530.3 吨 | 66.29% | 3 | 0.88 | 20.80% | 90.94 | 是 | 是 | 是 |
| T+60.0′ | 492.7 吨 | 61.59% | 3 | 0.88 | 25.50% | 91.38 | 是 | 是 | 是 |

**补充边界数据**

| 量 | 数值 | 说明 |
| --- | --- | --- |
| 判定阶段饱和度峰值 S_peak | **87.09%** | ΔS 的计算基准，出现于 T+32~41 区间 |
| 异常滞留指数峰值 | **0.88** | 3 台滞留车辆（车牌 赣C·A8823 / 赣C·B1290 / 赣C·C7734） |
| T+32.0′ 置信度四路分项 | a=91, b=90, c=93, d=86 | 加权 90.38，通过 |
| T+41.0′ 置信度四路分项 | a=92, b=91, c=93, d=86 | 加权 90.94，通过 |
| 全程无驶离轨迹 | 是（T+36.8′ 起持续成立） | 判定式 B 的必要条件 |

---

## 四、等价性与可验证性

源码包与旧版单文件实现经两轮自动校验，结论如下：

| 校验项 | 方法 | 规模 | 结果 |
| --- | --- | --- | --- |
| **数值等价** | `tools/equivalence-test.html`：逐时刻比对全部导出量（含 1201 个预计算采样点的 14 项指标） | **55,610 项** | **PASS，差异 0 项，最大绝对误差 0** |
| **接口覆盖** | 旧版 `window.PATENT` 的每个导出项，新版 `window.FWCore` 必须同名同类型 | 48 项 | PASS |
| **判定时刻** | 隐蔽囤货 32.0′ / 账面造假 40.6′ / 管控预警 41.0′ | 3 项 | 新旧完全一致 |
| **界面等价** | `bash tools/dom-diff.sh`：对 5 个页面 × 23 个冻结时刻，比对 DOM 文本 + 全部 ECharts 配置序列化 | **115 组** | **PASS，全部一致** |

> 说明：数值等价采用「不加载兼容层」的方式实现——`tools/equivalence-test.html` 先加载旧版 `patent-model.js`（建立基准 `window.PATENT`），再加载新版 `js/core/*`（建立待测 `window.FWCore`），从而可在同一页面内逐项比对。

---

## 五、权利要求建议对照

| 建议权利要求要点 | 支撑技术特征 | 关键实现位置 |
| --- | --- | --- |
| 一种烟花爆竹仓储**隐蔽囤货与账面造假风险的智能识别方法**（独立权利要求：五项步骤 S1~S5 的整体流程） | S0-2、S1-1、S2-4、S3-2、S4-1、S4-3、S5-2 | `js/core/06~11-*.js` |
| 由**扫码出入库数据与设计容量**计算归一化库存饱和度 S(t) | S1-1、S1-2 | `js/core/06-s1-saturation.js:27`、`:42` |
| 由**行政区划边界数据逐级解析**确定围栏圆心，构建地理电子围栏 | S2-1 | `js/core/03-geo-anchor.js:81`、`:90` |
| 由**停留时长与瞬时速度双条件**筛选滞留车辆，并计算**异常滞留指数** | S3-2、S3-3 | `js/core/08-s3-stagnation.js:55`、`:50-52`、`:66` |
| **双条件「与」逻辑**判定隐蔽囤货风险（饱和度与滞留指数双超阈） | S4-1 | `js/core/09-s4-dual-risk.js:39` |
| 以**判定阶段饱和度峰值**为基准计算账面下降幅度，配合**驶离轨迹缺失**判定账面造假风险 | S4-2、S4-3、S2-5 | `js/core/09-s4-dual-risk.js:49`、`:59`；`07-s2-geofence.js:70` |
| 判定结果**置位保持**，且仅记录首次命中时刻 | S4-5 | `js/core/11-timeline.js:48`、`:60`、`:70` |
| **多源数据置信度加权校验**通过后方确认风险并下发管控预警 | S5-1、S5-2 | `js/core/10-s5-confidence.js:45`、`:53` |
| **全时间线预计算**使风险判定与页面渲染帧序解耦 | S6-1、S6-2 | `js/core/11-timeline.js:40`、`:46` |
| 预警确认后在政务云 **GIS 分级图层**叠加显示仓储与车辆坐标 | S5-4 | `js/core/13-map-layers.js:44~189` |
| 单时刻**完整状态快照**聚合，保证多页面数据严格一致 | S6-4 | `js/core/12-aggregate.js:35` |

---

## 六、文件索引

| 文件 | 行数 | 内容 |
| --- | --- | --- |
| `js/core/00-namespace.js` | 47 | 命名空间与模块注册约定、成员冲突自检 |
| `js/core/01-params.js` | 60 | S0：阈值参数集 P、功能时间线 TL、考察窗口 BOOK_WIN |
| `js/core/02-geometry.js` | 58 | 平面几何基础函数 |
| `js/core/03-geo-anchor.js` | 99 | S2-1：目标仓储地理锚定 |
| `js/core/04-op-dataset.js` | 65 | S1-1、S4-4：扫码流水数据集（含 phantom 标记） |
| `js/core/05-vehicles.js` | 62 | S2-2、S3-1：车辆实体与进出场路径 |
| `js/core/06-s1-saturation.js` | 78 | S1-1~S1-3：库存饱和度计算 |
| `js/core/07-s2-geofence.js` | 93 | S2-3~S2-5：电子围栏与车辆轨迹 |
| `js/core/08-s3-stagnation.js` | 73 | S3-2~S3-4：滞留筛选与异常滞留指数 |
| `js/core/09-s4-dual-risk.js` | 67 | S4-1~S4-3：双风险判定式 |
| `js/core/10-s5-confidence.js` | 64 | S5-1~S5-3：多源置信度校验 |
| `js/core/11-timeline.js` | 112 | S6-1~S6-3、S4-5：时间线预计算 |
| `js/core/12-aggregate.js` | 98 | S6-4、S6-5：状态聚合与阶段机 |
| `js/core/13-map-layers.js` | 189 | S5-4：GIS 五图层构造 |
| `js/compat/export-patent.js` | 17 | 接口兼容层 |
| `js/render/page-shell.js` | 116 | 监测时钟、主循环、状态分发、自检钩子 |
| `js/render/boot.js` | 16 | 页面启动装配 |
| `js/pages/s1~s5.js` | 110 / 135 / 91 / 126 / 164 | 5 个功能页的渲染实现 |
| `pages/index.html`、`pages/s1~s5.html` | — | 6 个可直接打开的功能页面 |
| `tools/build-pages.py` | 232 | 页面组装脚本 |
| `tools/build-legacy.py` | 88 | 旧版对照基准生成脚本 |
| `tools/equivalence-test.html` | — | 数值等价性自检 |
| `tools/dom-diff.sh` | 103 | 界面等价性自检 |
